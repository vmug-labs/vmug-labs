# https://github.com/gruntwork-io/terragrunt/blob/master/README.md

terragrunt = {
  remote_state {
    backend = "s3"

    /*
    config {
      profile        = "aws_iam_access_key_profile"
      bucket         = "aws_s3_bucket"
      key            = "${path_relative_to_include()}/terraform.tfstate"
      region         = "us-west-2"
      encrypt        = true
      dynamodb_table = "aws_dynamodb_table"
    }
*/
  }
}
